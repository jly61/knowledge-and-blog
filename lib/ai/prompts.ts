/**
 * AI Prompt 模板
 */

/**
 * 基础对话系统提示词
 */
export const BASE_SYSTEM_PROMPT = `你是一个友好的 AI 助手，专门帮助用户管理知识库和博客。

你的职责：
- 回答用户的问题
- 帮助用户理解笔记内容
- 提供写作建议
- 协助内容创作

请用中文回复，保持友好和专业的语气。`

/**
 * 知识库上下文提示词（用于 RAG，第二阶段使用）
 */
export const RAG_SYSTEM_PROMPT = `你是一个知识库助手，可以访问用户的笔记内容。

当用户提问时，我会提供相关的笔记内容作为上下文。请基于这些内容回答问题。

如果问题超出了提供的上下文范围，请诚实说明，不要编造信息。`

/**
 * SEO 生成提示词
 *
 * 用于生成文章的 SEO 元数据（标题、描述、摘要）
 */
export const SEO_GENERATION_PROMPT = `请为以下文章生成 SEO 元数据。

文章标题：
{{TITLE}}

文章内容：
{{CONTENT}}

请生成以下内容（使用 JSON 格式返回）：
1. **metaTitle**: SEO 优化后的标题（50-60 字符，包含关键词，吸引点击）
2. **metaDescription**: SEO 描述（150-160 字符，用于 meta description，包含关键词和文章核心内容）
3. **excerpt**: 文章摘要（150-200 字符，用于文章预览和列表页显示）
4. **keywords**: SEO 关键词数组（3-5 个关键词，用逗号分隔）

要求：
- 标题要吸引人，包含核心关键词
- 描述要简洁明了，突出文章价值
- 摘要要概括文章主要内容
- 关键词要准确反映文章主题

请以 JSON 格式返回，例如：
{
  "metaTitle": "优化后的标题",
  "metaDescription": "SEO 描述",
  "excerpt": "文章摘要",
  "keywords": ["关键词1", "关键词2", "关键词3"]
}`

/**
 * 标签推荐提示词
 *
 * 用于根据文章/笔记内容推荐合适的标签和分类
 */
export const TAG_RECOMMENDATION_PROMPT = `请根据以下文章/笔记内容，推荐合适的标签和分类。

文章标题：
{{TITLE}}

文章内容：
{{CONTENT}}

已选择的标签：
{{EXISTING_TAGS}}

可用的标签列表：
{{TAGS_LIST}}

可用的分类列表：
{{CATEGORIES_LIST}}

请根据文章内容，从可用标签和分类中选择最合适的：

要求：
1. **标签推荐**：推荐 3-5 个最相关的标签（从可用标签列表中选择）
2. **分类推荐**：推荐 1 个最合适的分类（从可用分类列表中选择，可选）
3. **推荐理由**：为每个推荐的标签提供简要理由（可选）

注意事项：
- 只能从提供的可用标签和分类列表中选择
- 标签名称必须完全匹配（区分大小写）
- 如果内容与任何标签/分类都不匹配，可以返回空数组
- 避免推荐已选择的标签（除非确实非常相关）

请以 JSON 格式返回，例如：
{
  "tags": ["标签1", "标签2", "标签3"],
  "category": "分类名称",
  "reasons": {
    "标签1": "推荐理由1",
    "标签2": "推荐理由2"
  }
}

如果使用 Markdown 格式，请使用以下格式：
## 推荐标签
- 标签1：推荐理由1
- 标签2：推荐理由2
- 标签3：推荐理由3

## 推荐分类
- 分类名称：推荐理由`

